describe('Send a request to create event', () => {  
    it('hable to send Resquest', () => {
    
    });

    /*it('if even already exists', () => {
    });

    it('if service not exists', () => {
    });*/
});