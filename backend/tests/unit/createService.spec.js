describe('Create a service in mongodb', () => {  
    it('if all is ok', () => {
        
    });
    
    it('if service already exists', () => {
    });

    it('if name is invalid', () => {
    });
});